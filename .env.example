# Server environment variables (copy to server/.env)
# OpenAI API key for DALL-E image generation
OPENAI_API_KEY=sk-...

# Optional: Frontend URL for CORS (add your production URL)
FRONTEND_URL=https://your-production-domain.com
